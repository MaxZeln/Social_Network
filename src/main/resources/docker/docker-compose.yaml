version: '3.9'

services:
  postgres:
    image: postgres:14.2-alpine
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: "SocialNetwork"
      POSTGRES_USER: "user"
      POSTGRES_PASSWORD: password